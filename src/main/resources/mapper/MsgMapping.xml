<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingshuge.dao.MsgMapper">

    <select id="showMsg" resultType="Msg">
        SELECT msg.*,noveluser.*,pic.*
        FROM msg
        INNER JOIN pic ON msg.pic_id = pic.pic_id
        LEFT JOIN noveluser ON msg.user_id = noveluser.id
        WHERE msg.to_user_id =#{to_user_id} ;
    </select>

    <update id="reject" >
        update msg
        set show_flag = 0
        where user_id = #{user_id} and pic_id = #{pic_id}
    </update>

    <update id="rejectAll" >
        update msg
        set show_flag = 0
        where pic_id = #{pic_id}
    </update>

    <insert id="buy">
        insert into msg (user_id,to_user_id,pic_id) value (#{user_id},#{to_user_id},#{pic_id})
    </insert>


</mapper>