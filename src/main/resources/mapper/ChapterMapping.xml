<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingshuge.dao.ChaptersMapper">


<!--    Chapters showChapters(@Param("book_id") int book_id,@Param("chapters") int chapters);-->
    <select id="showChapters" resultType="Chapters">
        SELECT * FROM chapters WHERE book_id= #{book_id} and chapters=#{chapters}
    </select>

</mapper>